#include <iostream>

using namespace std;


/*
class Position {
public:
	int m_x, m_y;
	Position(int x, int y) : m_x(x), m_y(y) {}
};

class objSome {
public:
	Position p;		// 멤버로 객체를 가질 때 콜론 초기화
	objSome(int x, int y) : p(x, y) {}
	void show() {
		printf("%d %d\n", p.m_x, p.m_y);
	}
};

	int main() {

		objSome os(10, 30);
		os.show();


		return 0;
	}
*/